version: 2

# circlevi v2 config
defaults: &defaults
  docker:
    - image: circleci/clojure:lein-2.7.1
    - image: mysql:5.7
        environment:
        - MYSQL_ROOT_PASSWORD=zo8keiPaex
        - MYSQL_DATABASE=lenny_test



jobs:
  builder:
    <<: *defaults
    steps:
      - checkout
      - run: 
          name: Run Application Tests
          command: env
          environment:
          - DATABASE_URL: "jdbc:mysql://localhost/lenny_test?user=root&password=zo8keiPaex"
  test: 
    <<: *defaults
    steps:
      - run: ls


workflows:
  version: 2
  build-test:
    jobs:
      - builder
      - test:
          requires:
            - builder
