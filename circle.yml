machine:
  services:
    - docker

dependencies:
  pre:
    - sudo apt-get clean
    - sudo apt-get update
    - sudo apt-get install python-dev
    - sudo pip install awsebcli

deployment:
  production:
    branch: master
    commands:
      - lein uberjar
      - docker version
